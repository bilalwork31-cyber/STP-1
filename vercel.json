{
  "buildCommand": "cd frontend && npm ci && npm run build",
  "outputDirectory": "frontend/dist",
  "functions": {
    "api/index.py": { "excludeFiles": "{frontend/**,backend/trips/tests/**}" }
  },
  "rewrites": [{ "source": "/api/(.*)", "destination": "/api/index" }]
}
